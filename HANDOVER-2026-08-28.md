# Handover — 2026-08-28

> Written at Felix's request so tomorrow starts exactly where today stopped. The gap this fills is
> the one today exposed: sessions get interrupted, each one starts fresh, and nothing was writing
> down where the thinking had got to.

## Start here tomorrow

**Step 3, scenario one: better measurement of the same things.** Proposed rule is *accept, no
break* — the years still measure the same thing, measured better; step 4 catches it if the company
only applied it to some years.

The question left on the table, unanswered:

H&M's 2022 category 1 is described as *"Expanded supplier traceability including full tier 1 and 2
suppliers"* and moves **+1,952,000 tCO₂e, +38.5%**. Is improved visibility better measurement, or a
wider boundary?

- **Better measurement** — the emissions always existed, the company could not see them. Generous;
  keeps companies scoreable while they improve.
- **Boundary change** — what they count has changed, and 38.5% dwarfs any real decarbonisation in
  that period. Strict; consistent with "a fall in the line must mean a fall in emissions". Would
  break H&M's run at 2022.
- **Split by size** — refinement below a threshold, boundary change above it.

Claude's instinct was the second. Felix had not answered when the day ended.

## Also unanswered

Felix asked: *"did we not already do an external check to build the business model review?"*

**Answered properly on Felix's prompting — the first answer was too narrow.** The machinery exists
and is well developed. `src/lib/review.ts` in the ops app holds a four-pass relevance assessment,
where passes 1 to 3 are evidence and only pass 4 is an opinion, plus a `business_model` panel whose
every claim carries sources and which counts unsourced claims so an unsourced assessment announces
itself rather than looking finished.

It has been exercised on **exactly one company: Inditex.** `data_review` holds a single row —
pipeline company 25, the demo account, 3 August. Its sources are **invented by design**, because it
is the ops-004 demo seed (see `supabase/ops-014-business-model-sources.sql`).

So: one worked example, on the company the whole reporting-basis problem was discovered through,
built with fictional sources for demo purposes.

Felix's own account has **zero** `data_review` rows. No real business-model review exists for any
company, H&M included. H&M does carry a relevance override on category 2 marked `not_required` — so
a judgement was made and only its conclusion kept, without reasoning, sources or a date. That
matters for the validity-range design, which needs a date to run from and has none.

## The end-to-end review, and where it breaks

Felix, 2026-08-28: everything has to flow end to end when we test it. This is the chain a company
passes through, with what exists at each stage — checked against the code and the database today,
not recalled.

| # | Stage | State |
|---|---|---|
| 1 | Enters the pipeline | **Works.** 21 companies on Felix's account |
| 2 | Documents and figures captured | ⚠️ **Nothing runs it.** 17 of 21 sit in Queue at zero scrape attempts since 18 July. No cron, no timer — the "evening worker" the page refers to is not scheduled anywhere |
| 3 | Business model and value chain, four passes | **Built and well designed.** Exercised once, on Inditex, as the demo seed with invented sources. No real assessment for any company |
| 4 | Relevance per category, with a validity range | ⚠️ **Half built.** Per-year overrides exist and are used. The range that would let one determination cover many years does not |
| 5 | Restatement detection | **Works.** Ordered by publication, every restatement carrying the company's own words and a page-linked source |
| 6 | Restatement judgement | Steps 1, 2 and 4 settled today. **Step 3 open.** The review UI shipped today |
| 7 | A confirmed break sets the basis | ⚠️ **Missing entirely.** Judgements live in the ops database; nothing writes `basis_id` on the ThermoStat side. Every row is still basis 1, so the gate has never fired |
| 8 | Scoring | **Works, and already honours basis.** Consecutive years must share one; a run spanning two refuses with `basis_change`. Newest publication wins per year; the basket comes from the most recent year |
| 9 | Public score and methodology page | ⚠️ **Methodology page unwritten**, and the plan is explicit that the basis rule must not reach thermostat.earth before it exists |

**Three breaks stop an end-to-end test today: stage 2, stage 7, and stage 9.** Stage 7 is the
smallest of them and everything either side of it is built and waiting.

⚠️ **None of this is the first job tomorrow.** Felix was explicit: the restatement methodology comes
first, starting with step 3 scenario one, exactly where the section at the top of this document
stops. This map exists so the build order is obvious *once the methodology is settled* — it is not a
competing priority, and it should not be read as one. Rules before plumbing: building stage 7 before
step 3 is decided would wire up a judgement whose meaning is still being argued.

## Settled today

- **Steps 1, 2 and 4** of the restatement methodology — see `RESTATEMENT-METHODOLOGY.md`
- Order of review: business-model determination first, per year, restatements judged against it
- The order resolves into a sequence once relevance carries a validity range
- Two triggers re-open a determination: external checks, and methodology statements in reports
- The page 66 question: H&M stays at **one** failure, not three
- Sellpy is a boundary expansion, not a category move — Felix's original verdict stands

## Open, not settled

- Step 3, all six scenarios
- Whether relevance or restatements is settled first, given both now sit in a sequence
- Step 2's consequence for H&M category 11 — needs a business-model determination that does not exist

## State of the world

- **Ops app**: everything shipped and live, build `a9ab861`. Nothing waiting on `dev`.
- **Build-in-public pipeline**: stopped at Felix's request. All six cron jobs commented out, backup
  at `~/.felixep/backups/crontab-20260828.bak`. LinkedIn Buildlog archived in the sidebar, not
  deleted. The LinkedIn token expires 2026-10-27 with nothing left to warn about it.
- **ThermoStat repo**: methodology, decision record 0001, and this handover on `main`.
- **Not built**: relevance validity ranges, judgements reaching scoring, the two re-open triggers,
  a scheduled end-of-day handover.
