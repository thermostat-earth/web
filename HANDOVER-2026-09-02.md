# Handover — 2026-09-02

> Where today stopped and what tomorrow starts on. Written at the end of the session, from the git
> log and the database, not from memory.

## Start here tomorrow

**The write path is done** — this section originally said it was tomorrow's job, and it was
finished the same evening once Felix asked the right question.

Felix, 2026-09-02: *"I don't understand why a key is needed at all!!!! We've just been building
stuff on the same supabase project — is it not all just the same database mechanics?"* They are two
separate Supabase projects, in Ireland and Germany, so a call between them is a call between
systems. But the correction underneath the question was the useful one: the writer does not have to
be the ops app. **This machine already holds both databases' credentials, so if the write stays
here, no new key exists anywhere.** That is both the cheapest and the most secure of the options,
and it was already running an hour before it was proposed — a framing error on my part, not a
missing option.

So: `thermostat-apply-breaks.timer` every fifteen minutes, `thermostat-breaks-monitor.timer` hourly.
Unit files kept in `felixep-infra/thermostat/systemd/`.

## Tomorrow starts here: H&M's business-model determination

Felix, at the end of the session: *"do we not need to test the business model review?"* Yes, and it
is the last big dependency rather than a nice-to-have.

The machinery is good and has been run **once**, on Inditex, as the demo seed with invented sources
(`felixep-ops/supabase/ops-023-inditex-assessment.mjs`, 200 lines, and the standard to match). No
real company has a determination. H&M's page says 0 of 10 categories determined.

Today made it load-bearing twice: it decides required versus optional, which step 2's consequence
turns on, and it is what separates an added category from organic growth, which is the nuance Felix
added to scenario four. **Two rules settled today cannot fire without it.**

Doing it means fifteen categories for H&M, each establishing the activity from the business and
value chain *before* quoting what H&M says about it, every claim sourced to their report. Start
fresh; this is the work, not a warm-up to it.

**The other gaps, largest first:**

1. **Nothing captures new companies** — and the diagnosis is worse than "the worker is unscheduled".
   The worker has never existed: one commit in the repo's history has ever touched `scrape_status`
   and it added the board that displays it. `scraped_at` is null for all 42 companies, and **not one
   of Felix's queued companies has a source_url**. The missing piece was never the scraper; nothing
   knows which document to scrape. `felixep-infra/pipeline/find-sources.mjs` is a first attempt at
   that step and **has never been run** — its header says so.
2. **Nothing generates the AI recommendations** on restatements — a slot filled by hand.
3. **Two reports still unread**: Chanel's 2025 extract and H&M's 2025 annual report. Both are
   two-column tables the text extractor interleaves, so they need care rather than speed.
4. **The public methodology page** is unwritten, and the plan is explicit that the basis rule must
   not reach thermostat.earth before it exists.

## Settled today

- **All six of step 3's scenarios**, and they collapse to one test: was the change applied to every
  year in the run. See `RESTATEMENT-METHODOLOGY.md`.
- Most of it decided by **reading the standard**, not by arguing — chapter 5 of the Corporate
  Standard, section 9.3 and tables 5.8 and 9.5 of the Scope 3 Standard. Quoted in
  `STANDARDS-AND-RESTATEMENTS.md`. Felix's rule: check the source before asking him.
- **Adding a category only means restating if the business is the same business.** A new activity
  is organic growth and must show as an increase. The business-model review is what tells them
  apart, and it exists for no company — the largest dependency in the methodology.
- **H&M category 11 is optional by name**: table 5.8 lists apparel as indirect use-phase. The doc
  had said this could not be settled without a determination. It could.
- **A category move is decided on the company's statement**, not on the arithmetic. Other changes
  land in the same report, so the total moves either way.

## Built today

- Restatements carry the company's words and our note in **separate fields**, with a check
  constraint that refuses reviewer phrasing in the company's field. Four of seventeen restatements
  are unexplained, not one.
- A fourth verdict, **no acceptable explanation**, with the consequence for that company shown.
- **`reporting_basis_breaks`** — a confirmed break is a row, and `score_company` derives every
  year's basis from it. There is no "recorded but not applied" state left to monitor.
- Microsoft's 2025 fact sheet read in full (50 figures, reconciled to their stated subtotals) and
  ITV's 2021 report read at all, which gave ITV two years of history it did not have.

## Three defects, all the same shape

Each was correct code that had never run, and inert code is indistinguishable from working code:

1. The basis gate had never fired — every row was basis 1 from the day it was added.
2. The scoring read each year's basis from **scope 1 and 2 only**, ignoring scope 3 — where
   scenarios two, three and four all live.
3. A test that "passed" because the break it applied touched no scope 1 and 2 row at all.

**A check with nothing to check passes, and looks exactly like one that works.**

## Also built this evening

- **The server applies confirmed breaks**, every fifteen minutes, and Telegrams Felix whenever it
  actually moves a score. A job that changes a published number silently is the failure the bot
  watchdog taught us.
- **A monitor of the second kind**: hourly, it asks whether any confirmed decision is still not
  reaching the score. Tested by holding a break unapplied on purpose — it alerted, exited non-zero,
  and cleared when applied. An alarm nobody has heard is not known to work.
- Proven end to end by un-applying H&M's existing break and letting the timer's own service pick it
  up: applied as basis 2, judgement marked applied with the before and after scores, monitor clean,
  score unchanged at 1.5095.
- `~/.felixep/secrets/vercel.env` was world-readable while every other secrets file was not. Fixed.
- **"Review again"** on an approved company. The only route off Approved was Reopen, which sends it
  back to the Queue for a re-fetch that will never come, because nothing scrapes. Reviewing again
  keeps the data and reopens the judgement, which is the case when our rules changed rather than the
  data being wrong. The old button is renamed to say what it does.
